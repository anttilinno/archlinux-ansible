# Static Files

Place static files here that need to be copied to target systems.

Files in this directory can be referenced using the `copy` or `template` modules with just the filename (no path needed).

